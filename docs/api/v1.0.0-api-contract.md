# SaveTogether V1.0.0 — API Contract Reference

**Base URL:** `/api/v1`  
**Authentication:** JWT Bearer Token (unless marked PUBLIC)  
**Content-Type:** `application/json`  
**Version:** 1.0.0  

---

## 1. Authentication

| Method | Endpoint | Auth | Description |
|:---|:---|:---|:---|
| POST | `/auth/send-otp` | PUBLIC | Send SMS OTP to phone number |
| POST | `/auth/verify-otp` | PUBLIC | Verify OTP, return JWT + refresh token |
| POST | `/auth/refresh` | PUBLIC | Refresh access token using refresh token |
| POST | `/auth/logout` | JWT | Invalidate session and refresh token |

---

## 2. Users

| Method | Endpoint | Auth | Description |
|:---|:---|:---|:---|
| GET | `/users/profile` | JWT | Get authenticated user profile |
| PUT | `/users/profile` | JWT | Update user profile |
| POST | `/users/profile/avatar` | JWT | Upload avatar |
| DELETE | `/users/profile` | JWT | Deactivate account |

---

## 3. Societies

| Method | Endpoint | Auth | Description |
|:---|:---|:---|:---|
| GET | `/societies` | PUBLIC | List all active societies |
| GET | `/societies/:id` | PUBLIC | Get society details |
| GET | `/societies/search` | PUBLIC | Search societies by name/city |
| GET | `/societies/:id/blocks` | PUBLIC | List blocks in a society |

---

## 4. Addresses

| Method | Endpoint | Auth | Description |
|:---|:---|:---|:---|
| GET | `/addresses` | JWT | Get user's addresses |
| POST | `/addresses` | JWT | Add new address |
| PUT | `/addresses/:id` | JWT | Update address (ownership enforced) |
| DELETE | `/addresses/:id` | JWT | Remove address (ownership enforced) |
| PATCH | `/addresses/:id/default` | JWT | Set default address |

---

## 5. Service Catalog

| Method | Endpoint | Auth | Description |
|:---|:---|:---|:---|
| GET | `/services/categories` | PUBLIC | List service categories |
| GET | `/services` | PUBLIC | List all active services |
| GET | `/services/:id` | PUBLIC | Get service details |
| GET | `/services/featured` | PUBLIC | List featured/popular services |

---

## 6. Pricing Engine

| Method | Endpoint | Auth | Description |
|:---|:---|:---|:---|
| POST | `/pricing/quote` | JWT | Get server-calculated pricing quote |
| GET | `/pricing/tiers/:serviceId` | PUBLIC | Get pricing tier table for a service |

**Request (POST `/pricing/quote`):**
```json
{
  "serviceId": "string",
  "societyId": "string",
  "quantity": "number"
}
```

**Response:**
```json
{
  "currentTier": { "min": 20, "max": 39, "unitPrice": 599 },
  "currentPrice": 599,
  "nextTier": { "min": 40, "max": 59, "unitPrice": 549 },
  "nextPrice": 549,
  "remainingForNextTier": 22,
  "potentialSavings": 50
}
```

---

## 7. Demand Engine

| Method | Endpoint | Auth | Description |
|:---|:---|:---|:---|
| GET | `/demand/opportunities` | JWT | Active demand campaigns for user's society |
| GET | `/demand/campaigns` | PUBLIC | List active demand campaigns |
| GET | `/demand/campaigns/:id` | PUBLIC | Get campaign details with demand count |

---

## 8. Demand Campaign (Admin)

| Method | Endpoint | Auth | Description |
|:---|:---|:---|:---|
| GET | `/admin/demand/campaigns` | ADMIN | List all demand campaigns |
| GET | `/admin/demand/campaigns/:id` | ADMIN | Campaign detail with bookings |
| POST | `/admin/demand/campaigns/:id/process` | ADMIN | Move campaign to processing |
| POST | `/admin/demand/campaigns/:id/cancel` | ADMIN | Cancel demand campaign |

---

## 9. Bookings

| Method | Endpoint | Auth | Description |
|:---|:---|:---|:---|
| POST | `/bookings` | JWT | Create new booking (price snapshot) |
| GET | `/bookings` | JWT | List user's bookings |
| GET | `/bookings/:id` | JWT | Booking details (ownership enforced) |
| GET | `/bookings/:id/history` | JWT | Booking state transition history |
| POST | `/bookings/:id/cancel` | JWT | Cancel booking |
| POST | `/bookings/:id/reschedule` | JWT | Reschedule booking |

### Booking Admin

| Method | Endpoint | Auth | Description |
|:---|:---|:---|:---|
| POST | `/admin/bookings/:id/process` | ADMIN | Move to processing |
| POST | `/admin/bookings/:id/assign-vendor` | ADMIN | Assign vendor |
| POST | `/admin/bookings/:id/schedule` | ADMIN | Schedule service date |
| POST | `/admin/bookings/:id/complete` | ADMIN | Mark as completed |

---

## 10. Payments

| Method | Endpoint | Auth | Description |
|:---|:---|:---|:---|
| POST | `/payments/create-order` | JWT | Create Razorpay payment order |
| POST | `/payments/verify` | JWT | Verify payment signature |
| GET | `/payments/:id` | JWT | Get payment details |
| POST | `/payments/webhook` | PUBLIC | Razorpay webhook (HMAC verified) |

---

## 11. Refunds

| Method | Endpoint | Auth | Description |
|:---|:---|:---|:---|
| GET | `/refunds/:id` | JWT | Get refund details |
| GET | `/admin/refunds` | ADMIN | List all refunds |
| GET | `/admin/refunds/:id` | ADMIN | Refund detail |
| POST | `/admin/refunds/:id/retry` | ADMIN (FINANCE) | Retry failed refund |

---

## 12. Vendors

| Method | Endpoint | Auth | Description |
|:---|:---|:---|:---|
| GET | `/vendors` | ADMIN | List all vendors |
| POST | `/vendors` | ADMIN | Register new vendor |
| GET | `/vendors/:id` | ADMIN | Vendor details |
| PATCH | `/vendors/:id` | ADMIN | Update vendor profile |
| POST | `/vendors/:id/services` | ADMIN | Add service to vendor |
| POST | `/vendors/:id/pricing` | ADMIN | Set vendor pricing |
| GET | `/vendors/:id/price-history` | ADMIN | Vendor price history |
| GET | `/vendors/:id/availability` | ADMIN | Get vendor availability |
| POST | `/vendors/:id/availability/block` | ADMIN | Block vendor dates |
| POST | `/vendors/:id/availability/unblock` | ADMIN | Unblock vendor dates |
| PATCH | `/vendors/:id/capacity` | ADMIN | Update vendor capacity |
| POST | `/vendors/availability/eligible` | ADMIN | Get eligible vendors |

---

## 13. Vendor Assignment

| Method | Endpoint | Auth | Description |
|:---|:---|:---|:---|
| GET | `/vendor-assignments` | ADMIN | List all assignments |
| GET | `/vendor-assignments/:id` | ADMIN | Assignment details |
| POST | `/vendor-assignments/:id/confirm` | ADMIN | Confirm assignment |
| GET | `/demand-campaigns/:id/vendor-options` | ADMIN | Eligible vendors for campaign |
| POST | `/demand-campaigns/:id/assign-vendor` | ADMIN | Assign vendor to campaign |

---

## 14. Vendor Negotiations

| Method | Endpoint | Auth | Description |
|:---|:---|:---|:---|
| POST | `/demand-campaigns/:id/negotiations` | ADMIN | Start negotiation |
| GET | `/demand-campaigns/:id/negotiations` | ADMIN | List campaign negotiations |
| GET | `/negotiations/:id` | ADMIN | Negotiation details |
| POST | `/negotiations/:id/offers` | ADMIN | Submit counter-offer |
| POST | `/negotiations/:id/accept` | ADMIN | Accept negotiation |
| POST | `/negotiations/:id/reject` | ADMIN | Reject negotiation |

---

## 15. Referrals

| Method | Endpoint | Auth | Description |
|:---|:---|:---|:---|
| GET | `/referrals` | JWT | Get user's referral code + stats |
| GET | `/referrals/history` | JWT | Referral history |
| POST | `/referrals/validate` | PUBLIC | Validate referral code |
| POST | `/referrals/attribute` | JWT | Attribute referral to booking |
| GET | `/admin/referrals` | ADMIN | Admin referral overview |
| POST | `/admin/referrals/:id/fraud-review` | ADMIN | Flag/review referral |

---

## 16. Rewards

| Method | Endpoint | Auth | Description |
|:---|:---|:---|:---|
| GET | `/rewards/wallet` | JWT | Get wallet balance |
| GET | `/rewards/transactions` | JWT | Transaction history (ledger) |
| POST | `/rewards/redemptions` | JWT | Redeem rewards |

---

## 17. Notifications

| Method | Endpoint | Auth | Description |
|:---|:---|:---|:---|
| GET | `/notifications` | JWT | Get user's notifications |
| PATCH | `/notifications/:id/read` | JWT | Mark notification as read |
| POST | `/notifications/fcm-token` | JWT | Register FCM push token |

---

## 18. Support / Complaints

| Method | Endpoint | Auth | Description |
|:---|:---|:---|:---|
| POST | `/support/complaints` | JWT | Submit complaint |
| GET | `/support/my-complaints` | JWT | User's complaint history |
| GET | `/admin/complaints` | ADMIN | All complaints |
| PATCH | `/admin/complaints/:id` | ADMIN | Update complaint status |

---

## 19. Reviews

| Method | Endpoint | Auth | Description |
|:---|:---|:---|:---|
| POST | `/reviews` | JWT | Submit review (1 per booking) |
| GET | `/reviews/service/:serviceId` | PUBLIC | Reviews for a service |
| GET | `/reviews/my-reviews` | JWT | User's reviews |
| GET | `/admin/reviews` | ADMIN | All reviews (moderation) |
| PATCH | `/admin/reviews/:id` | ADMIN | Approve/reject review |

---

## 20. Admin Authentication

| Method | Endpoint | Auth | Description |
|:---|:---|:---|:---|
| POST | `/admin/auth/login` | PUBLIC | Admin login |
| POST | `/admin/auth/2fa/verify` | PUBLIC | Verify 2FA challenge |
| POST | `/admin/auth/logout` | ADMIN | Admin logout |

---

## 21. Admin Console

| Method | Endpoint | Auth | Description |
|:---|:---|:---|:---|
| GET | `/admin/overview` | ADMIN | Executive dashboard summary |
| GET | `/admin/users` | ADMIN | List all users |
| GET | `/admin/bookings` | ADMIN | List all bookings |

---

## 22. Analytics

| Method | Endpoint | Auth | Description |
|:---|:---|:---|:---|
| GET | `/analytics/summary` | ADMIN | Executive summary |
| GET | `/analytics/revenue` | ADMIN | Revenue report |
| GET | `/analytics/bookings` | ADMIN | Booking trends |
| GET | `/analytics/demand` | ADMIN | Demand analytics |

---

## 23. Settings

| Method | Endpoint | Auth | Description |
|:---|:---|:---|:---|
| GET | `/settings` | ADMIN | Get all platform settings |
| PUT | `/settings` | ADMIN | Update settings (versioned) |
| GET | `/settings/history` | ADMIN | Settings change history |

---

## 24. Audit Log

| Method | Endpoint | Auth | Description |
|:---|:---|:---|:---|
| GET | `/audit/logs` | ADMIN | Query audit log (append-only) |
| GET | `/audit/logs/:id` | ADMIN | Specific audit entry |

---

## 25. Health & Monitoring

| Method | Endpoint | Auth | Description |
|:---|:---|:---|:---|
| GET | `/health` | PUBLIC | Basic health check |
| GET | `/health/readiness` | PUBLIC | Infrastructure readiness probe |
| GET | `/monitoring/metrics` | ADMIN | Technical metrics |
| GET | `/monitoring/business` | ADMIN | Business metrics |
| GET | `/monitoring/alerts` | ADMIN | Active system alerts |

---

## Error Response Format

All error responses follow this structure:

```json
{
  "success": false,
  "error": {
    "code": "ERROR_CODE",
    "message": "Human-readable message",
    "details": {}
  },
  "requestId": "uuid"
}
```

## Standard HTTP Status Codes

| Code | Usage |
|:---|:---|
| 200 | Success |
| 201 | Created |
| 400 | Validation error |
| 401 | Not authenticated |
| 403 | Forbidden (ownership / RBAC) |
| 404 | Resource not found |
| 409 | Conflict (duplicate / state violation) |
| 429 | Rate limited |
| 500 | Internal server error |
